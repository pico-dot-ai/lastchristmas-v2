{
  "version": 2,
  "builds": [
    { "src": "web/next.config.mjs", "use": "@vercel/next" }
  ],
  "installCommand": "cd web && npm ci",
  "buildCommand": "cd web && npm run build",
  "routes": [
    { "src": "/(.*)", "dest": "/web/$1" }
  ]
}
