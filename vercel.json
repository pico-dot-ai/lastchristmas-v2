{
  "version": 2,
  "builds": [
    { "src": "web/package.json", "use": "@vercel/next" }
  ],
  "installCommand": "cd web && npm ci",
  "buildCommand": "cd web && npm run build",
  "routes": [
    { "src": "/(.*)", "dest": "/web/$1" }
  ]
}
